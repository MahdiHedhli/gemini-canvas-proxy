{
    "manifest_version": 3,
    "name": "Gemini Canvas Proxy Bridge",
    "version": "1.0.0",
    "description": "Routes API requests through Gemini Canvas for free unlimited inference. Uses a private MessageChannel bridge — no WebSocket or Local Network Access issues.",
    "permissions": [
        "alarms",
        "storage",
        "tabs",
        "nativeMessaging"
    ],
    "background": {
        "service_worker": "background.js"
    },
    "content_scripts": [
        {
            "matches": ["https://gemini.google.com/*"],
            "js": ["content_script.js"],
            "run_at": "document_start",
            "all_frames": true
        }
    ]
}
